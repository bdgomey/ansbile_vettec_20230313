plugin: aws_ec2

regions:
  - us-east-1

hostnames:
  # This option allows displaying the public ip addresses.
  - ip-address
  # - tag:Name

# keyed_groups:
# - key: tags['CreatedBy']
#   prefix: bjgomes

groups:

  bjgomes: tags.CreatedBy

filters:
  tag:CreatedBy: bjgomes